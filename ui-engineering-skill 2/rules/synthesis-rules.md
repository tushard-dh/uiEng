# Synthesis Rules

Product requirements > accessibility/security > design system > UX conventions
> explicit user preference > reference preference > AI preference.

Resolve conflicts explicitly and persist the decision.
